{
  "name": "cf-photos",
  "version": "1.0.0",
  "description": "基于 Cloudflare Workers + WebDAV 的高性能图床应用。",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:ci": "wrangler deploy; s=$?; node scripts/notify-bark.mjs $s; exit $s",
    "test": "node --test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.68.1"
  },
  "dependencies": {
    "fast-xml-parser": "^5.11.1"
  },
  "type": "module"
}
